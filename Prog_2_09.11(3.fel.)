import numpy as np

def main():
    m=int(input("Sorok szama: "))
    n=int(input(" Oszlopok szama: "))
    a=np.zeros((m,n))
    for i in range(0, m):
        for j in range(0, n):
            a[i][j]=i*j
    print(a)
main()
